/** Mirrors backend config.settings.BOOKING_DEPOSIT_RATE default; used only for a client-side estimate
 * before submission — the authoritative amount is always computed server-side. */
export const DEFAULT_DEPOSIT_RATE = 0.3;
